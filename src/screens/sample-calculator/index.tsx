import { Text } from "react-native";
import { Container } from "@/common";

export const SampleCalculator = () => {
    return (
        <Container>
            <Text>Em breve...</Text>
        </Container>
    );
};
